compiler: g++
dist: xenial
language: cpp
os: linux

stages:
  - build
  - test
  - deploy

env:
  - TARGET=armv7
  - TARGET=x86_64
        
jobs: 
  include: 
    - stage: build
      script: sleep 5; echo "build armv7 "
    - script: sleep 5; echo "build x86_64"
    - stage: test
      script: sleep 5; echo "test 1 "
    - script: sleep 5; echo "test 2"
    - stage: deploy
      script: sleep 5; echo "deploy "

